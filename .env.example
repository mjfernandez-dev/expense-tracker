# ============================================
# FINANZAAPP - CONFIGURACIÓN DE ENTORNO
# ============================================
# Copiar este archivo a .env y completar con valores reales
# NO incluir este archivo en control de versiones si tiene datos sensibles

# ============================================
# SEGURIDAD - REQUERIDO EN PRODUCCIÓN
# ============================================

# Clave secreta para firmar JWT (generar con: python -c "import secrets; print(secrets.token_urlsafe(32))")
SECRET_KEY=tu-clave-segura-de-32-caracteres-cambiar-en-produccion

# Ambiente (development o production)
ENVIRONMENT=development

# ============================================
# BASE DE DATOS - PostgreSQL
# ============================================

# Usuario de PostgreSQL
DB_USER=gastos_user

# Contraseña de PostgreSQL (REQUERIDA)
DB_PASSWORD=tu-contraseña-segura-cambiar

# Nombre de la base de datos
DB_NAME=gastos_db

# Puerto de PostgreSQL (por defecto 5432)
DB_PORT=5432

# URL completa de conexión (se construye automáticamente desde arriba en docker-compose)
# Solo usar esto si quieres una BD externa
# DATABASE_URL=postgresql://gastos_user:password@localhost:5432/gastos_db

# ============================================
# EMAIL - Para reset de contraseña
# ============================================

# Proveedor SMTP (ejemplo: smtp.gmail.com, smtp.sendgrid.net)
SMTP_HOST=smtp.gmail.com

# Puerto SMTP (típicamente 587 para TLS, 465 para SSL)
SMTP_PORT=587

# Usuario/email para autenticar en SMTP
SMTP_USER=tu-email@gmail.com

# Contraseña de SMTP (para Gmail: usar App Password, no contraseña regular)
SMTP_PASSWORD=tu-asword-smtp-cambiar

# Email remitente (puede ser igual a SMTP_USER)
SENDER_EMAIL=noreply@finanzaapp.local

# ============================================
# ENCRIPTACIÓN (opcional)
# ============================================

# Clave de encriptación para datos sensibles
# Generar con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# ============================================
# CONFIGURACIÓN ADICIONAL
# ============================================

# Puerto backend FastAPI
BACKEND_PORT=8000

# Habilitar debug mode (NO en producción)
DEBUG=false
